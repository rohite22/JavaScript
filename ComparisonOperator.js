//Learning comparison or relational operator
let x= 80
let y = 21 //The values within quotes is considered as string type

//let result = x==y; //The value of x is compared with value of y
// console.log('the result is :' +result)
// console.log(result)
console.log(x >= y) //the === will check the data type also.

/*Ternary operator are liek if-then-else

it will accept 3 arguments

condition ? value if true : valueif false */

let result = x > y ? x : y
console.log(result)
